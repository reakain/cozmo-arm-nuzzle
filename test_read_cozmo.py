#!/usr/bin/env python
# Test getting camera frames from cozmo using pycozmo
import pycozmo
from matplotlib import pyplot as plt

# initialize a cozmo

# connect to our cozmo

# get a camera frame
# frame = ????

# display a camera frame using plt
# plt.imshow(frame)
# plt.title('Cozmo Frame'), plt.xticks([]), plt.yticks([])

# plt.show()
